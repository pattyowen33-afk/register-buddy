node_modules
dist

# Environment files — set these in Railway dashboard instead
.env
.env.local
.env*.local
server/.env

# SQLite database (rebuilt by scraper on deploy)
server/db/*.db
server/db/*.db-shm
server/db/*.db-wal
